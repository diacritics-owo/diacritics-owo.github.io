<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ site.title }} {{- if title }} - {{ title -}} {{ /if }}</title>
    <link href="/styles/main.css" rel="stylesheet" inline />
  </head>
  <body>
    {{ comp.navigation() }}
    <main class="p-20 pt-10 flex">
      <div class="flex-none w-40 sticky top-0 self-start pt-20 prose">
        {{ contents }}
      </div>
      <article class="prose flex-1 max-width-[800px] m-auto">
        {{ content }}
      </article>
      <div class="flex-none m-auto"></div>
    </main>
    <div class="h-[50dvh]"></div>
  </body>
</html>
